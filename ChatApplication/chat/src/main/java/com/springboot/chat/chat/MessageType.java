package com.springboot.chat.chat;

// Enum representing different types of chat messages.
public enum  MessageType {


    // Represents a regular chat message.
    CHAT,

    // Represents a message indicating a user has joined the chat.
    JOIN,

    // Represents a message indicating a user has left the chat.
    LEAVE
}
